import React from 'react'

const Header: React.FC = () => {
    return (
        <header style={{ textAlign: 'center', background: 'black', color: '#fff' }}>
            <p>メモ帳アプリ</p>
        </header>
    )
}

export default Header
